"""CLI entry stub."""

from __future__ import annotations


def main() -> None:
    """Console script entry (`photoreal`). Placeholder only."""
    print(
        "photoreal CLI placeholder — implement subcommands that call "
        "photoreal.app.invoker.Invoker."
    )


if __name__ == "__main__":
    main()
