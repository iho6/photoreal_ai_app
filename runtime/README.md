# Runtime

Engine hosts live here (ComfyUI, other local GPU servers). Product code under `photoreal/` should talk to engines through `services/`, not by importing this tree.

| Path | Purpose |
|------|---------|
| `comfyui/` | ComfyUI checkout or install root (placeholder) |
